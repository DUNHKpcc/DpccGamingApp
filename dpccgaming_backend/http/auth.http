@baseUrl = http://localhost:8080
@username = testuser01
@password = 12345678

### 1) 正常注册
POST {{baseUrl}}/api/register
Content-Type: application/json

{
  "username": "{{username}}",
  "password": "{{password}}"
}

### 2) 重复用户名（执行一次1后再执行）
POST {{baseUrl}}/api/register
Content-Type: application/json

{
  "username": "{{username}}",
  "password": "{{password}}"
}

### 3) 参数非法（用户名太短）
POST {{baseUrl}}/api/register
Content-Type: application/json

{
  "username": "ab",
  "password": "{{password}}"
}

### 4) 登录（从响应里复制 token）
POST {{baseUrl}}/api/login
Content-Type: application/json

{
  "username": "{{username}}",
  "password": "{{password}}"
}

### 5) 验证 token（把下面 token 替换成第4步返回值）
@token = REPLACE_WITH_LOGIN_TOKEN
GET {{baseUrl}}/api/verify-token
Authorization: Bearer {{token}}

### 6) 获取用户资料
GET {{baseUrl}}/api/user/profile
Authorization: Bearer {{token}}

### 7) 获取当前用户（兼容路由 /api/me）
GET {{baseUrl}}/api/me
Authorization: Bearer {{token}}

### 8) 无 token 访问受保护接口（应返回 401/403）
GET {{baseUrl}}/api/me
